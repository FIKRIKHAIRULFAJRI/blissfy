export type Collection = {
  id: string;
  title: string;
  description: string;
  href: string;
  tone: "warm" | "deep";
};

export type ServicePromise = {
  title: string;
  description: string;
};
